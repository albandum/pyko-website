---
interface Props {
  quote: string;
  author: string;
  role: string;
  company: string;
  photo?: string;
  class?: string;
}

const { quote, author, role, company, photo, class: className = '' } = Astro.props;
---

<div class={`p-8 rounded-xl bg-white border border-gray-200 card-shadow-lg ${className}`}>
  <!-- Quote Icon -->
  <div class="mb-4">
    <svg class="w-10 h-10 text-accent/20" fill="currentColor" viewBox="0 0 24 24">
      <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
    </svg>
  </div>

  <!-- Quote -->
  <blockquote class="text-lg text-gray-700 mb-6 leading-relaxed">
    "{quote}"
  </blockquote>

  <!-- Author Info -->
  <div class="flex items-center gap-4">
    {photo ? (
      <img
        src={photo}
        alt={author}
        class="w-12 h-12 rounded-full object-cover"
      />
    ) : (
      <div class="w-12 h-12 rounded-full bg-accent/10 flex items-center justify-center text-accent font-bold text-lg">
        {author.charAt(0)}
      </div>
    )}
    <div>
      <div class="font-semibold text-navy">{author}</div>
      <div class="text-sm text-gray-600">{role}</div>
      <div class="text-sm text-gray-500">{company}</div>
    </div>
  </div>
</div>
